# 開発記録（UI操作レクチャー資料 / ui-kit）

`CHANGELOG.md` が「何が入ったか」の一覧なのに対し、この記録は **「なぜそうしたか」「何が壊れていて、どう直したか」「どこまで確かめたか」** を残す。
後から仕様を変えるとき、あるいは同種の資料を別領域で作るときに、判断の根拠をたどれるようにするのが目的。

対象ブランチ: `claude/fable-ui-operations-guide-8ehgke` ／ 記録時点: 2026-09-06
差分規模: `origin/main` から **42 ファイル / +4,969 行 / −65 行**（生成物を含む）

---

## 1. 目的と、最初に置いた制約

**目的**：フロントエンド UI の「操作の名称」と「実際の動き」が結びついていない状態を解消する教材を作る。
読み手は製造業の業務改善担当（kintone のレコード蓄積と API、Electron 製業務アプリの開発が主領域）。

最初に決めた制約と、その理由：

| 制約 | 理由 |
|---|---|
| **バニラ JS / HTML、依存ゼロ** | 現場の PC で `npm install` を通せるとは限らない。ダブルクリックで開けることを最優先した |
| **`file://` で動く** | 社内サーバや配布基盤を前提にすると、読める人が減る |
| **表示コード＝実行コード** | 「資料に載っているコード」と「実際に動いているコード」がずれると教材として破綻する。`<style data-code>` / `<script data-code>` を `assets/guide.js` がその場で転記する方式にした（写しではないので、開発者ツールで書き換えるとその場で挙動が変わる） |
| **数値は実測して表示する** | 「速い / 重い」を言葉で書くと信用できない。フレーム時間・対応可否・件数は、その端末で測って出す |
| **推測値を書かない** | kintone の API 上限などは公式仕様の確認が要る。把握値であることを明示し、断定しない |

この4つが、以降のほぼ全ての設計判断（単一ファイル化、模擬デモの扱い、3D の依存方針）を決めている。

---

## 2. 成果物の現状

| 区分 | 数量 |
|---|---|
| 教材 HTML ページ | 20 |
| カタログ項目 | **143**（A 22 / B 20 / C 20 / D 16 / E 8 / M 15 / X 12 / Y 14 / Z 16） |
| 動くデモ（プレイグラウンドに載る単位） | 151 |
| 横断検索の索引 | 195 件 |
| 単一ファイル版 | `dist/ui-guide-standalone.html`（52 ファイル収録、**約 1.34 MB**、外部参照ゼロ） |
| 部品ライブラリ | `ui-kit/` 16 モジュール（`dist/ui-kit.js` は約 38 KB） |
| 自動テスト | 教材 **122 件** ／ ui-kit **34 件** |

層の構成：

- **A〜C**：一般 → カスタマイズ → プロ志向。標準機能で賄える操作から、自前で制御する操作へ
- **D**：業務アプリの設計パターン（A〜C の部品の組み合わせ方）
- **E**：演出・体感の上位効果
- **M**：A〜C で自前実装した処理が標準機能になったもの（`:has()`、popover、アンカー位置指定 ほか）
- **X**：クロスプラットフォーム（端末差の実測と吸収）
- **Y**：サードパーティ連携
- **Z**：3D と AR

---

## 3. 段階ごとの経緯（コミット単位）

### `abffb32` 学習体験の改善
プレイグラウンド（各デモの HTML/CSS/JS をその場で編集して実行）、横断検索、「理解した」チェック、ダークモード。
デモのコードを **`build.mjs` がページから抽出**して `assets/demo-data.js` を生成する方式にした。教材本体とプレイグラウンドの内容が二重管理にならない。

### `bd5b27a` 学習の道筋
カリキュラム（到達目標つき）、下書き自動保存、クイズの「動きを見て当てる」モード、`ui-kit/index.d.ts`（`tsc --strict` で検証）。

### `ef36f4a` / `6da374b` 単一ファイル化
資料一式を外部参照ゼロの 1 枚にまとめる。**同じ処理をブラウザ（`16-bundler.html`）と Node（`build.mjs`）の両方から呼び、出力がバイト一致することを確認**した。
「Node が無い環境でも配布物を作れる」という要件を、教材自身の題材にもしている。

### `bc4ce0b` 第5弾（M層・X層）
最新 CSS/JS と端末差。**この段で、既存デモに実害のある日本語入力（IME）の不具合が複数見つかった**（後述）。

### `02afb50` 素案
Y層・Z層の追加計画。2026 年時点のブラウザ状況を調べたうえで、実装方針の選択肢を提示した。

### `c7160ae` 第6弾（Y層・Z層）
外部連携 14 項目と 3D/AR 16 項目、および依存ゼロの WebGL2 レンダラ `assets/mini3d.js`。

### `4961b97` 追記（監査）
「不足はないか」を自分で監査し、**プレイグラウンドの実害バグ2件**と、**抜けていた4項目**を補った（後述）。

### `bd16338` `ui-kit/retry.js`
122（レート制限と再試行）を再利用部品として切り出した。

---

## 4. 主要な設計判断と、その理由

### 4.1 「模擬」と「実動」を見出しで区別する
相手のサーバ・OS・実機が必要な項目は、ブラウザ内で完結しない。ごまかさず **`模擬` / `実動` のバッジを見出しに出し**、模擬側は「動きを再現したうえで実コードを併記」する形にした。
模擬にしたのは Y層の 117（3rd party Cookie の遮断）・120（Electron の認証）など。

### 4.2 3D は「自前実装 ＋ ライブラリ参考コードの併記」
選択肢は3つあった。

| 案 | 利点 | 欠点 |
|---|---|---|
| 自前 WebGL2 のみ | 依存ゼロ・単一ファイル配布を壊さない・原理が分かる | 実務でそのまま使う人はいない |
| three.js / model-viewer のみ | 実務に近い | CDN 依存で `file://` とオフラインが壊れる。原理が見えない |
| **併用（採用）** | デモは依存ゼロで動き、実務のコードも読める | 書く量が増える |

採用した「併用」の具体形：`assets/mini3d.js`（行列演算・形状生成・レイキャスト・GPU カラーIDピッキング・クリッピング・射影・オービット）で **全16項目が実際に動き**、各項目に `three.js なら` / `<model-viewer> なら` の参考コードを併記する。

### 4.3 ピッキングは2方式を実測で比較させる
レイキャスト（CPU、Möller–Trumbore）と カラーID（GPU、`readPixels`）を同じ画面に並べ、実測値を出す。
どちらが速いかは**場面による**（レイキャストは三角形数に比例、カラーID は読み戻しの同期待ちが固定でかかる）ため、断定せず測らせる作りにした。

### 4.4 署名検証は ui-kit に入れない
Y層 123（Webhook の受け側）は HMAC 署名の検証を `crypto.subtle` で実際に計算するが、**部品としては切り出さなかった**。
理由：あれは本来サーバ側のコードで、ブラウザ部品ライブラリに置くと「クライアントで検証すればよい」と誤読される恐れがある。再利用部品にしたのは再試行（`retry.js`）だけ。

### 4.5 kintone の上限値は数値を書かない
1回の取得件数・更新件数・同時リクエスト数などは、**把握値であることを明示し、公式 REST API リファレンスで確認するよう促す**にとどめた。
資料に数値を書くと、それが独り歩きして現場の設計根拠になってしまうため。

---

## 5. 見つけて直した不具合

教材そのもののバグと、教材が題材にしているコードのバグの両方がある。**原因 → 修正 → 再発防止**の順で記録する。

### 5.1 日本語入力（IME）の実害（第5弾で発見）
X層 107 を書くために既存デモを点検したところ、**実際に壊れている箇所が4つ**見つかった。

| 箇所 | 症状 | 原因 | 修正 |
|---|---|---|---|
| 74 検索 | 変換中に検索が走る | `input` は変換確定前にも発火する | `e.isComposing` で無視し、`compositionend` で1回だけ走らせる |
| 36 コマンドパレット | 変換中の矢印・Enter を操作として拾う | 同上（`keydown` も変換中に飛ぶ） | 変換中は無視 |
| 38 インライン編集 | **変換確定の Enter で確定してしまう** | `keydown` の `isComposing` を見ていない | 変換中は無視 |
| `ui-kit/hotkeys.js` | 変換中のキーでショートカットが発火 | 同上 | `e.isComposing \|\| e.keyCode === 229` で早期 return |

さらに 38 には**別の不具合**があった：確定した Enter が `td` へバブリングし、`td` のハンドラが編集を再開していた。`e.target === td` の判定を足して解決。
→ いずれも回帰テストを `verify.mjs` に追加済み。日本語の業務アプリで最も実害が出る箇所なので、テストで固定した。

### 5.2 単一ファイル版が実行されない（第8弾）
生成した 1 枚を開いても中身が動かない。
**原因**：埋め込んだランタイムの中に、正規表現リテラルとして書いた `<!--` と `<script` の文字列があり、HTML パーサが「二重エスケープ状態」に入って `</script>` が閉じタグとして扱われなかった。
**修正**：タグに見える並びを**すべて `\x3c` で書く**。加えて `bundler.js` に、生成物のシェル部分に `</script` 等が現れたら生成を止めるガードを入れた。

### 5.3 単一ファイル版でプレイグラウンドが壊れる
**原因**：置換で挿入した文字列が、JS のテンプレートリテラルの内側に生の `</script>` を持ち込んでいた。さらに `html.replace('</body>')` が、テンプレートリテラル内部にある**最初の**出現にヒットしていた。
**修正**：`\x3c\\/script>` を出力してテキストとして `<\/script>` を保つ。差し込み位置は `lastIndexOf('</body>')` に変更。

### 5.4 トーストの無限ループ
`while (children.length >= max) dismiss(first)` が終わらない。
**原因**：`dismiss` の DOM 除去がアニメーション完了後だったため、`children.length` が減らない。
**修正**：表示中の要素を別配列 `live[]` で持ち、**同期的に**そこから外す。

### 5.5 並べ替えの往復（FLIP）
ドラッグ中に要素が行ったり来たりする。
**原因**：アニメーション中の兄弟要素に対して当たり判定をしていたため、位置が確定する前に再判定が走る。
**修正**：`animating` の `WeakSet` を持ち、当たり判定から除外（`ui-kit/sortable.js` とデモ 32 の両方）。

### 5.6 デモ98（`scheduler.yield()`）の計測が 0ms
**原因**：フレーム監視を処理完了と同時に止めていたため、最悪フレームを取り逃していた。
**修正**：完了後 300ms まで監視を続けてフレームを数える。結果、意味のある比較（1600ms 対 167ms）が出るようになった。

### 5.7 ホットスポットの遮蔽判定が常に「隠れている」（139）
**原因**：レイの `hit.distance`（ニアプレーンからの距離）と、視点からラベル点までの距離を**別基準のまま**比較していた。
**修正**：距離ではなく**交点そのもの**とラベル点を比べる（`vec3.length(vec3.sub(hit.point, s.p)) > 0.05`）。

### 5.8 GPU ピッキングが 38ms かかる（132）
**原因**：呼び出しのたびにフレームバッファを作り直していた。
**修正**：`ensurePickTarget(w, h)` でキャッシュ。あわせて、残る `readPixels` の同期待ちが何なのかを本文で正直に説明するようにした（速く見せるより、何が起きているかを説明する方が教材として正しい）。

### 5.9 プレイグラウンドで 3D デモが全滅（監査で発見）
**症状**：Z層のデモをプレイグラウンドで開くと `ReferenceError: Mini3D is not defined`。
**原因**：プレイグラウンドの iframe には `guide.css` と `guide.js` しか読ませていなかった。単一ファイル版も同様。
**修正**：3D のデモのときだけ `assets/mini3d.js` を差し込む。単一ファイル版では埋め込み済みの中身（`window.__A.mini3d`）を使う。

### 5.10 `.stage` の外にある操作ボタンが抽出されない（監査で発見）
**症状**：一部のデモがプレイグラウンドで `Cannot set properties of null`。
**原因**：`build.mjs` は `.stage` だけを切り出していたが、`.stage` の**外**に置いたボタン・出力欄を参照するデモがあった。
**修正**：`.stage` に続く**兄弟の `<div class="row">` も取り込む**ようにし、21-3d の 128・140 のマークアップも整理。
**再発防止**：**全 151 デモをプレイグラウンドで実行し、エラーが 0 件であることを毎回検証する**テストを追加した。以後この種の取り込み漏れは自動で止まる。

### 5.12 IME 対策の漏れが 4 か所残っていた（検査ツールを作って発見）
5.1 では「気づいた場所」を直しただけで、**網羅していなかった**。検査ツール `tools/check-ime.mjs` を作って全ファイルを走査したところ、同型の未対策が4か所見つかった。

| 箇所 | 症状 |
|---|---|
| `00-index.html` 横断検索 | 変換中の未確定文字で検索が走る |
| `05-advanced.html` 81 ワークスペース | 変換中に絞り込みと再描画が走る |
| `05-advanced.html` 82 ルーティング | 同上 |
| `ui-kit/example/index.html` 実例アプリ | 同上 |
| `14-playground.html` Ctrl/⌘+Enter | 変換中にも実行される（`hotkeys.js` と同型） |

**教訓**：この種の横断的な欠陥は、目視では必ず取りこぼす。**検査を機械化して初めて網羅できる**。
ツールは誤検出も出す（canvas・range・下書き保存など変換が起きない場所）ので、`check-ime-ignore` コメントで理由付きの除外を残す運用にした。除外の理由がコードに書かれることで、次に読む人が同じ判断をやり直さずに済む。

### 5.11 単一ファイル版のサイズ表示が実際と違う（この記録の作成中に発見）
`build.mjs` が `html.length`（**文字数**）を MB として出していたため、日本語（1文字3バイト）を多く含む本資料では実サイズと大きくずれていた（1.07 対 実際 1.34 MB）。
`Buffer.byteLength(html, 'utf8')` に変更し、`16-bundler.html` が表示する `Blob.size` と一致させた。README の記載も実測値に直した。

---

## 6. テスト環境側の落とし穴（教材のバグではないもの）

記録しておかないと同じ時間を溶かすため、区別して残す。

- `p.mouse.click` に**スクロール前のページ座標**を渡していた（canvas が y≈3062 にあり、クリックが別の場所に落ちる）→ `scrollIntoViewIfNeeded()` してから `boundingBox()` を取る
- ギズモのハンドルを画面走査で探そうとして失敗 → `canvas.__mini3d` を公開し、**ハンドルの3D座標を `project()` して**クリック位置を出す
- Playwright を使うテストは `NODE_PATH=/opt/node22/lib/node_modules` が要る
- `retry.js` の中断テストで `signal` を渡し忘れ、テスト側の誤りをコードの不具合と誤認しかけた（待機中に `abort()` する形に書き直した）
- 124（反映方式）の検証を、ポーリング間隔（2秒）ちょうどの固定待ちで判定していたため**まれに落ちた**。`waitForFunction` で追いつくまで待ち、回数の閾値も余裕のある値に直した（テストの不安定さは、教材の不具合と見分けがつかなくなるので潰しておく）

---

## 7. 検証の方法と結果

```
npm run build       # ui-kit/*.js → ui-kit/dist/ui-kit.js
npm run build:docs  # デモ抽出・検索索引・収録一覧・単一ファイル版を再生成
npm test            # ui-kit のブラウザテスト        → 34 passed / 0 failed
npm run test:docs   # 教材ページの検証               → 122 passed / 0 failed
```

`test:docs` が見ているもの：

- 生成物が最新か（`demo-data.js` / `search-index.js` / `manifest.js` が現在のページと一致するか）
- 全20ページの JS エラー・リンク切れ・デモ数
- 各層の代表操作（M層の対応バッジ、X層の実測、Y層の再試行・署名検証・反映方式、Z層のピッキング・カメラ）
- **IME 対策の回帰**（5.1 の4件）
- A7 のキーボード操作、クイズの層選択
- **全 151 デモのプレイグラウンド実行**（5.10 の再発防止）
- 単一ファイル版が 1 枚だけで動くか（ナビ・デモ・ページ間リンク・プレイグラウンド・ui-kit 実例）

`build.mjs` は、スクリプトがあるのに `.stage` を取り出せないセクションを見つけると**警告して終了コードを 1 にする**（`<div>` の閉じ忘れを早期に検出するため）。

---

## 8. 既知の制約・未確認事項

| 項目 | 状態 |
|---|---|
| kintone の API 上限値 | **数値は書いていない**。公式 REST API リファレンスで要確認 |
| Y層 117 / 120 | 相手サーバ・OS が要るため模擬。実コードは併記 |
| Z層 126（WebHID / WebSerial / WebUSB） | Chromium 系のみ。接続ボタンは**実機がある環境でのみ完走**する。実機なしでも「キーボード型リーダーの判定」は試せる |
| Z層 142（AR） | 実起動には HTTPS と対応端末が要る。ページ上は対応可否の実測表示まで |
| 61・62（Electron のフレームレス／ネイティブメニュー） | ブラウザ上では動かないため模擬 |
| 動作確認ブラウザ | Chromium 系で実施。View Transitions・popover 等は非対応環境でフォールバックする |
| 50（マルチタッチ） | 本来はタッチ端末での2本指操作。マウスでは Shift+ドラッグで代用 |

---

## 9. 今後の入口

- **Mission Bridge（Electron アプリ）への適用**：`docs/ui-guide/07-implementation-plan.md` に手順と確認事項がある。ただし対象リポジトリは未確定（本記録時点では「一旦置いておく」との判断）
- **ui-kit の追加候補**：現時点で部品化したのは 16 モジュール。Y層からは `retry.js` のみを切り出した（4.4 の理由による）
- **Z層の発展**：`mini3d.js` は教材用の最小実装。実務では 143 の比較表に従って three.js / model-viewer / web-ifc を選ぶ
